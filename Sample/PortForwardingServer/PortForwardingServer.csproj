<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>14.0</LangVersion>
    <Description>SSH port forwarding / tunneling example — TCP forwarding policy (All/None/Local/Remote), remote and local port forwarding with SSH.NET client, data flow through tunnels. ApacheMinaSSHD.NET by SERALYNX LLC (Porta SFTP Server).</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\ApacheMinaSSHD.NET.Wrapper\ApacheMinaSSHD.NET.Wrapper.csproj" />
    <PackageReference Include="SSH.NET" Version="2025.1.0" />
  </ItemGroup>

</Project>
