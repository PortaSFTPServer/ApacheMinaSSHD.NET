<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>14.0</LangVersion>
    <Description>SFTP and SCP client/server example — starts a full SSH/SFTP/SCP server, then connects via SSH.NET SftpClient and ScpClient to upload, download, and list files. ApacheMinaSSHD.NET by SERALYNX LLC (Porta SFTP Server).</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\ApacheMinaSSHD.NET.Wrapper\ApacheMinaSSHD.NET.Wrapper.csproj" />
    <PackageReference Include="SSH.NET" Version="2025.1.0" />
  </ItemGroup>

</Project>
