<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <LangVersion>14.0</LangVersion>
    <Description>C# SFTP server authentication example — password, public key, fingerprint, authorized_keys, keyboard-interactive, and MFA. Full SSH authentication with multiple methods. ApacheMinaSSHD.NET by SERALYNX LLC (Porta SFTP Server).</Description>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\..\ApacheMinaSSHD.NET.Wrapper\ApacheMinaSSHD.NET.Wrapper.csproj" />
  </ItemGroup>
</Project>

